{
  "type": "array",
  "items": {"$ref": "#/definitions/XmlExercise"},
  "definitions": {
    "XmlExercise": {
      "allOf": [
        {"$ref": "../exercise.schema.json"},
        {
          "properties": {
            "rootNode": {"type": "string"},
            "grammarDescription": {"type": "string"},
            "samples": {"type": "array", "items": {"$ref": "#/definitions/XmlSample"}}
          },
          "required": ["rootNode", "grammarDescription", "samples"],
          "additionalProperties": false
        }
      ]
    },
    "XmlSample": {
      "type": "object",
      "properties": {
        "id": {"type": "number"},
        "grammar": {"type": "string"},
        "document": {"type": "string"}
      },
      "required": ["id", "grammar", "document"]
    }
  }
}
