{
  "type": "array",
  "items": {"$ref": "#/definitions/XmlExercise"},
  "definitions": {
    "XmlExercise": {
      "allOf": [
        {"$ref": "../exercise.schema.json"},
        {
          "properties": {
            "grammarDescription": {"type": "string"},
            "rootNode": {"type": "string"},
            "samples": {"type": "array", "items": {"$ref": "#/definitions/XmlSample"}}
          },
          "required": ["grammarDescription", "rootNode", "samples"],
          "additionalProperties": false
        }
      ]
    },
    "XmlSample": {
      "type": "object",
      "properties": {
        "id": {"type": "number"},
        "sample": {"$ref": "#/definitions/XmlSolution"}
      },
      "required": ["id", "sample"]
    },
    "XmlSolution": {
      "type": "object",
      "properties": {
        "grammar": {"type": "string"},
        "document": {"type": "string"}
      },
      "required": ["grammar", "document"]
    }
  }
}
